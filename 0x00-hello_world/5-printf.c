#include <stdio.h>

/**
 * main -  prints the phrase with proper grammer,
 * but the outcomeis a piece of art
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);


}
