#include "main.h"

/**
 * factorial - return factorial
 * @n: the int to be calculated
 * Return: results
 */

int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
