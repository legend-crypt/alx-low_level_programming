#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcmp - compares strings
 * @s1: the first string
 * @s2: the second string
 * Return: string
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
