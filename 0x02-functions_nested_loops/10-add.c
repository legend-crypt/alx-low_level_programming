#include "main.h"
/**
 * add - print the result of two added numbers
 * @a: first parameter
 * @b: second parameter
 * Return: int.
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
